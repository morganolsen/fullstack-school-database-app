import React from 'react';

/**
 * Loading component to display loading text when loading data.
 */
export default function Loading() {
    return(
        <div align="center">
            <h2>Loading...</h2>
        </div>
    );
}